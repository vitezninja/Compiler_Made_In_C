import "importedLib";

struct Word {
    char letter;
}

union RGB {
    int8 red;
    int8 green;
    int8 blue;
}

enum State {
    IDLE,
    WALKING,
    DEAD,
}

State enemyState;

(void) die(State entity) {
    entity = DEAD;
}

(int32) main() {
    die(enemyState);
}